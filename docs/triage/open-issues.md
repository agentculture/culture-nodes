# Open-issue triage

Generated by `python3 scripts/triage-report.py`. Do not edit this file by hand.

| Issue | Bucket | Disposition | Evidence pointer |
|---:|---|---|---|
| #6 | owner decisions | cost and decide the OIDC workload-authentication lane | plan t27 |
| #10 | verify-then-close | verify bridge follow-ups on current HEAD | plan t5-t7 |
| #12 | owner decisions | cost and decide the remaining Web and UX scope | plan t27 |
| #87 | large bets | deliver through the named end-to-end ownership program | plan t9-t11/t23 |
| #88 | finish work | widen the quality gate after Go CI exists | plan t1/t20 |
| #89 | large bets | schedule scope-think-challenge workflow as a named large bet | close-the-backlog large-bets batch |
| #93 | operator-lane enablers | automate checkout preparation in the operator lane | plan t9/t26 |
| #97 | large bets | schedule first-class subscription budgets as a named large bet | close-the-backlog large-bets batch |
| #109 | large bets | deliver as the named autonomous-cycle outcome | plan t28 |
| #110 | owner decisions | cost and decide the managed Postgres trial | plan t16/t27 |
| #111 | finish work | ship simple expiring credential form then track replacement | plan t21 |
| #112 | owner decisions | choose and document the database target | plan t15/t16/t27 |
| #113 | large bets | schedule containerized bridge contract as a named large bet | close-the-backlog large-bets batch |
| #114 | large bets | schedule repository-configured agents as a named large bet | close-the-backlog large-bets batch |
| #116 | bug tail | fix the attempt timestamps that make duration_percentiles report zeros | found verifying #28 in run 01M03BV3DYNB9N7J1Q1RJ55HNB |
| #117 | finish work | audit every origin-stamping site against the actor registry | found building t30 in run 01M03DPMXFN912FF9BG6M2NJYJ |
| #118 | large bets | make the combining step a node, then the Jira-driven loop around it | 15 hand-merges in the close-the-backlog cycle; STATE section 11 |
| #119 | owner decisions | pick one of four options for database-backed Go in the codex lane; option 1 needs an AF_UNIX probe first | run 01M04C39ZQK8NHJPQFSG8Z3QNV: socket(2) returns EPERM for loopback and egress alike |
| #120 | bug tail | a bridge that cannot hand over must refuse the dispatch that asks; and attempted-but-empty must be distinguishable from not-attempted | three dispatches reported handover=true and created no ref; fixed by redeploy, found only by for-each-ref on the host |
| #121 | finish work | apply migration 0036 after the dial-in cutover; it is written and deliberately held unapplied | t24 landed partial; merging it into the applied sequence dropped endpoint_ref and failed 14 tests |
| #125 | bug tail | decide where repo comes from for a multi-lane bridge: a dedicated single-lane actor, or actor-registry metadata | instruction half closed in d7c6677; run 01M04Q26ZPNKTXVNBGSDS1YR9F still blocked on the developer bridge's two allowlist entries |
| #128 | operator-lane enablers | redeploy thor and both codex bridges from main after #122 squash-merges, then reconcile the two by-hand prod.env edits and drop the pre-deploy dump | successor to #127; squash means 431528d is not an ancestor of main, so prod is verified by /v1alpha1/version matching main HEAD |
| #129 | bug tail | decide whether the missing runner-operation orphan sweep is a deliberate trade-off or a gap, then record or close it | runnerasync.go:408 states there is no unstick step, no orphan sweep, and no lease to expire; found by code read during t7, not reproduced |
| #133 | bug tail | make a FORCE_PROD rotation and NODES_DATABASE_URL unable to disagree — either the rotation refreshes the URL, or the audit reports the divergence by name | found reviewing #137's settings lane before merge; the implementing agent flagged the behaviour change rather than letting it pass as a pure addition |
| #135 | bug tail | stop writing a DATABASE_SSLMODE that can contradict the URL beside it, and settle whether env_has or env_get is right about duplicate keys | reported by the test agent on #137 from a test it wrote and deliberately did not assert on; hostname literals are pre-existing |
| #136 | operator-lane enablers | re-register the five spark-hosted actors at the current LAN address and give the host a reservation; evidence for the #121 dial-in cutover, not a shortcut around it | GET /v1alpha1/actors lists five actors at 192.168.1.157, which no longer resolves; dispatch to every spark actor is failing now |
| #140 | owner decisions | decide whether the product is renamed; nothing in this cycle depends on the answer | rename proposal, untouched by the upkeep-dispatch cycle |
| #141 | large bets | scope whether full-duplex push is worth a dependency in five zero-dependency adapters; measure the long poll's latency floor first | deferred deliberately once direction of initiation, not framing, proved to be what removes a stored address |
| #145 | operator-lane enablers | implement the execution-overlap instrumentation the t20 disposition chose | docs/decisions/dialin-duplicate-execution.md; decided, not built |
| #147 | operator-lane enablers | give dial-in settings a per-bridge home; everything downstream in the cutover is blocked on it | dialin.configured reads os.environ only and spark's four claude bridges share one EnvironmentFile |
| #155 | large bets | decide whether the cycle itself becomes flows: intake from Jira, scope/think/challenge as ledger-backed nodes, a plan that compiles to a published graph, and sweeps that turn observations into events instead of issues | written after #154; carries the measured backlog growth (every day since 2026-08-08 net-positive) and the six issues this round closed as records rather than work |
| #162 | bug tail | give dispositions() its own exception so a malformed table exits 1 (a finding) instead of 2 (could not measure) | scripts/triage-report.py:355-367 + the comment at :365 that already states the intended rule |
| #171 | finish work | build the artifact GET/consume side with the POST's custody discipline; unblocks decision c25 frame-as-artifact travel | internal/artifacts Router.Get with no HTTP caller + deviation d2 |
| #172 | bug tail | rewrite examples/pr-upkeep/README.md against the v2 event-driven workflow it ships beside | examples/pr-upkeep/README.md v1 nodes vs workflow.yaml v2 |
| #173 | finish work | give a signal wait a declared timeout edge (reminder/escalation on silence); timer rows already exist, the wait-park path just never writes one | internal/worker/wait.go decodeUntil + examples/jira-question-round-trip header gap note |
| #175 | large bets | placement redesign: actors register tags, a selection policy with recorded placement, h21's tag-pinned acceptance demonstrated end to end | internal/worker/concurrency.go package doc + deviation d5 |
| #178 | bug tail | choose fix 1 (durable attempts row at dispatch) or 2 (lenient attempt_ref) and land it; blocks every graph-dispatched gate verdict | internal/api/gatereports.go + migration 0009 design + t8 demo runs 01M09ZZV/01M0A0KW/01M0A0R8 |
| #181 | bug tail | make the actor ceiling exact: reserve capacity inside the invocation-insert transaction; doc states approximate posture meanwhile | internal/worker/concurrency.go package doc + PR #180 review |
| #182 | bug tail | extend t12's lost-resume recovery to the codex async path (classification, ForkEvent, cold re-brief), then all-backends mirror | adapters/codex/src/codex_bridge/server.py:596-659 + PR #180 review |
| #183 | bug tail | refuse early on identity mismatch (no redispatch), surface the FK cause on the run; consider stamping server-side identity per #117 | worker log FK trio on run 01M0A3RW + internal/api actor callback custody |
| #186 | finish work | give namespace creation a real API route (POST /v1alpha1/namespaces), replacing the raw SQL insert register-actor.sh and deploys lean on | issue #186 body + deploy/prod/register-actor.sh namespace lookup |
| #200 | finish work | the page, its route and signals 3 and 8 are delivered and proven on SCRUM-5 (runs 3, replies 2, latest_frame v27); held open on a MEASURED failure, not a gap: signal 9 milestone 2 produced TWO link comments (10191, 10205). WP-I 2f2778f makes the link an engine-owned singleton but prod still serves 5d9be94. Closes on: deploy the tip, then one link comment across two intake milestones on a NEW ticket | issue #200; docs/deliveries/2026-08-29-jira-flow-spec-read-related-bugs.md; SCRUM-5 comments 10191/10205 |
| #203 | finish work | run the flow-store build cycle to its final PR then close on the delivery record | issue #203 |
| #204 | owner decisions | scope the lane-tag model against the actor registry and #48/#28 surfaces | issue #204 |
| #205 | bug tail | the fix is delivered and mirrored across all adapters (52453dd backoff+warn-once on 401; f9a2e69 the developer unit reads its issued credential). Held open because a Bug closes when it stops reproducing and the reproduction check has not run: presence 'connected' does not disprove a 1/s loop underneath it. Closes on honesty condition h7 - a one-hour journalctl read of culture-nodes-claude-developer counting 401s, naming the revision it was taken against | issue #205; docs/deliveries/2026-08-29-jira-flow-spec-read-related-bugs.md (only medium-confidence claim) |
| #218 | large bets | fold first-party skill script logic into the nodes CLI so skills instruct rather than implement; vendored skills keep their scripts | issue #218 |
| #221 | owner decisions | decide where the SonarCloud boundary lives — a nodes verb, or a written split — then make the callers obey it | issue #221 |
| #226 | large bets | large bet: do not open cold. Start with the compose-to-compose check #224 already needs, then registry-to-compose, then aggregate the revision surfaces that already exist (/v1alpha1/version, each bridge's deployment block). Decide first whether this is `nodes doctor --mesh` or a sibling verb -- doctor is deliberately local and its checks=4 is gated. Explicitly NOT a private mirror of the deployment checkouts | issue #226 |
| #230 | operator-lane enablers | the cycle is merged (#244 #246 #238) and #193 #194 #197 #198 #202 #207 #231 #240 #242 closed on its evidence 2026-08-29. Still owns the prod tail: deploy the integration tip (first end-to-end run of WP-J's sourced lanes and #246's per-call preflight doctor), re-measure signal 9 on a new ticket (#200), read the developer journal for 401s (#205), decide human task 01M16FX0BWK9X6TKE9BHAAW88Y, add jira_bot_account_id to PR_UPKEEP_REPOSITORIES and re-grant. Prod measured at 5d9be94 on 2026-08-29; signal 2 stays unverifiable on this site (#235) | issue #230; docs/deliveries/2026-08-29-jira-flow-spec-read-related-bugs.md; docs/audits/2026-08-29-fleet-and-bridge-evaluation-jira-flow-cycle.md |
| #232 | large bets | the store-pull second control plane (spec h17/c19): spark's dev plane with its own postgres and actor registrations; pull one prod-proven entry and run it. Unscoped setup work through the whole flow-store cycle; the delivery summary carries the claim as unverified until a run id exists on a second plane | issue #232; docs/deliveries/2026-08-19-jira-operating-surface-flow-store.md |
| #235 | finish work | follow-up to #200 as redefined 2026-08-29: page replies post as the system's Jira account first; this Task gives them the user's own identity (Jira OAuth / signed link, or a registered human actor via human-inbox). Depends on #200 and the LAN-reachability decision parked on the frame | issue #235; adapters/human-inbox/README.md |
| #237 | large bets | deferred by q4 of the jira-flow cycle: the headspace code-node form of the spec chain needs a committed move program, artifact GET through the runner boundary, and a checkout (gaps 2/3/5 in examples/spec-chain/workflow.yaml); the lane-session form ships first under #199 | issue #237; examples/spec-chain/workflow.yaml |
| #239 | large bets | deferred from the jira-flow cycle (PR #238 Qodo finding 1, plan risk r6): subject-scoped signal subscriptions are an engine feature (until.signal subject + signal_subscriptions column + matching change); the cycle neutralises the name-wide wake consumer-side via question_correlation.answer_for and a decoy-run test on t10 | issue #239; internal/store/postgres/signal.go selectLockedSubscriptionsSQL; examples/jira-question-round-trip/question_correlation.py |
| #241 | operator-lane enablers | surface the contract_rejected reason on the attempt record, the attempt.completed event, nodes-op run and the web run view; the mode-binding bug cost a workflow-source diff and an engine read to diagnose | issue #241; internal/worker/bindings.go:276 |
| #243 | large bets | operator direction 2026-08-29: agents work freely as dedicated OS users per host instead of docker/bwrap sandboxes; the evidence table on the issue maps every wave-0/1 lane defect of the jira-flow cycle (no network, no PG, no formatters, no loopback, no npm, uncommitted handover, killed sessions) to run ids and gate commits. Start with deploy.sh creating the users and one bridge unit running as its user | issue #243; #230 hand-turns 3-6 |

Open issues with dispositions: 55

## Issue types

Types are read per-issue from GitHub at render time, never from the search `type:` qualifier (it fails open on an unknown name and lags writes). Typing began 2026-08-17: every count below is a cut over a backlog that predates adoption, so an untyped issue means nobody has typed it yet, not that it has no kind.

### Open issues by type

| Type | Issues | Typing began |
|---|---:|---|
| Task | 19 | 2026-08-17 |
| Bug | 14 | 2026-08-17 |
| Feature | 22 | 2026-08-17 |
| Record | 0 | 2026-08-17 |
| (no type) | 0 | 2026-08-17 |

### Issues closed since 2026-08-15 by type

| Type | Issues | Typing began |
|---|---:|---|
| Task | 8 | 2026-08-17 |
| Bug | 18 | 2026-08-17 |
| Feature | 10 | 2026-08-17 |
| Record | 18 | 2026-08-17 |
| (no type) | 40 | 2026-08-17 |
