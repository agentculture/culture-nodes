# qwen-bridge (placeholder)

`pyproject.toml` points at this README (modeled on the sibling adapters);
hatchling refuses to build the project without the file existing. The real
content — trust model, the ACP seam, the invocation-input table, the
capability surface, the build+run runbook — lands in plan task t7.
