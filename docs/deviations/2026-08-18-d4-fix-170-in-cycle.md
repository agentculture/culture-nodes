# Deviation d4 — fixing #170 inside the cycle (2026-08-18)

Proposed deviation d4 on the `jira-driven-idea-to-shipped-loop` plan
(`devague deviate --list`), classification `acceptable`, awaiting human
confirmation.

## What deviated

The confirmed plan has no task for #170 (a code node's resolved input is
refused-or-discarded — it can reject a dispatch but never reach the
process). The cycle adds its fix as in-scope work.

## Why

Every honest-gap list this cycle produced converged on the same blocker:
t18's spec-chain gap 1, and t5's combining-loop authoring, where the
harvest/stage/merge code nodes need per-run values (handover ref, candidate
SHA, verdict) that cannot reach them today. Without the fix, t5's graph
compiles but t8's live demonstration cannot feed a single code step —
"documented around" is not runnable. The fix is small at the measured seam
(`buildCodeOperation` lowers `dc.Input` into the operation environment as
`NODES_INPUT_JSON`), is a Bug repair rather than new design, and lands
test-first with the no-input dispatch byte-identical to today.

## Effect on the plan

- t5's program (`scripts/combining-loop-node.py`) reads `NODES_INPUT_JSON`.
- t8 and t20's demonstrations become feedable end to end.
- #170 closes when this branch reaches main, citing the fix.
