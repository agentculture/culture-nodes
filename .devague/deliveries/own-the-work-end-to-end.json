{
  "plan_slug": "own-the-work-end-to-end",
  "schema_version": 1,
  "created": "2026-08-15T06:38:50Z",
  "updated": "2026-08-15T07:56:24Z",
  "deviations": [
    {
      "id": "d1",
      "what": "Serialize package dispatch to one package per codex actor at a time: wave 0 runs as two rounds (round 1: P1->codex-thor, P2->codex-orin; round 2: P3->codex-thor, P4->codex-orin) instead of four concurrent packages. Waves 1-3 follow the same one-per-actor rule until t16 lands. Task content, acceptance criteria and coverage targets are unchanged \u2014 this is a scheduling amendment only.",
      "task_ref": "t1",
      "reason": "Each codex bridge allowlists exactly ONE checkout per host, exact-match \u2014 verified live: thor's codex-bridge.json has repo_allowlist=['/home/thor/git/culture-nodes-agent'] and config.py resolves membership by equality. Two concurrent packages on one actor would therefore put two agent sessions in one checkout, which is precisely the concurrent-writer corruption mode spec claim c42 describes, with the c49 fence not yet built (it is task t10). Running it would reproduce, while building the fix, the exact hazard the batch exists to remove. Serializing costs wall-clock and nothing else; t16 (the workspace provisioner, already scheduled in wave 1) restores parallelism for waves 2-3, and this incident is empirical evidence for t16's value rather than an argument for it.",
      "affects": [
        "t3",
        "t4",
        "t5",
        "t8",
        "t15",
        "t16",
        "c42",
        "c49",
        "c24"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d2",
      "what": "Refresh both codex hosts' allowlisted checkouts to main before dispatching any package.",
      "task_ref": "t1",
      "reason": "thor's /home/thor/git/culture-nodes-agent is at fadfa1d, two commits behind main 7519d74 \u2014 so an agent would build against stale code and its diff would not apply cleanly at merge. Observed at wave-0 dispatch; orin unchecked and assumed equally stale until verified.",
      "affects": [
        "t3",
        "t5"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d3",
      "what": "Commit and push the spec, the plan, the devague frame/plan/delivery state and scripts/verify-token-scope.sh to a branch, and have every subsequent work-package brief reference them via 'git fetch origin && git show origin/<branch>:<path>' instead of inlining the task text into the instruction string.",
      "task_ref": "t1",
      "reason": "Discovered at wave-0 dispatch: run 01M022BSJ3550RQ8Q2P7DFWA91 refused to start because 'docs/plans/2026-08-15-own-the-work-end-to-end.md is absent from this checkout and all local Git refs'. The plan was uncommitted on spark, so the brief referenced a path that does not exist on thor \u2014 the same class of defect #74 documents, committed by the operator while dispatching its fix. The workaround (inline every task's full text into the instruction string) does not scale past a two-task package and makes the ledger see one opaque blob instead of citable documents. Committing and pushing costs one branch and fixes the class for every remaining package. The operator approved a cheap in-flight fix via /deviate. Note this is a WORKAROUND for the missing artifact carrier, not a substitute: it works only because these artifacts belong in git anyway. A brief attachment that is not a repo file still has no carrier \u2014 that is #79, and the improvement issue stands.",
      "affects": [
        "t3",
        "t5",
        "t16",
        "c2",
        "c69"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d4",
      "what": "Wave-0 round 2 blocked on sandbox posture: under codex --sandbox workspace-write the WORKTREE is writable but .git is READ-ONLY, so git fetch/commit/branch all fail ('.git/FETCH_HEAD: Read-only file system'). Both P2 (run 01M023GXENMQ0QRZQH5G61TXD9) and P3 (run 01M023GXJB2ARP1B8213Q992AT) reported it independently after their write probes passed. Packages must be re-dispatched with a sandbox that permits git metadata writes, per the agent checkout's own AGENTS.md guidance (danger-full-access).",
      "task_ref": "t1",
      "reason": "This refines #18 rather than reopening it: file writes in the worktree ARE proven (three independent probes), but git-metadata writes are not permitted under workspace-write. The consequence reaches the spec, not just the fan-out: the git_ref handover carrier decided in q9/c70 requires the producing agent to create a commit and a ref, which is exactly what this sandbox forbids. So the two-carrier decision currently depends on either raising the sandbox to danger-full-access for any package that hands over a ref, or on the operator creating the ref on the agent's behalf \u2014 which reintroduces the human-as-transport the batch exists to remove. Classified RISKY because raising the sandbox weakens the confinement that made read-only dispatch safe, and that trade deserves an explicit decision rather than being absorbed as a dispatch flag.",
      "affects": [
        "t3",
        "t4",
        "t8",
        "t15",
        "c69",
        "c70"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "risky"
    },
    {
      "id": "d5",
      "what": "Unblock wave 0 without changing sandbox posture: dispatch remaining packages with SELF-CONTAINED briefs under --sandbox workspace-write, rather than raising to danger-full-access so agents can git fetch the plan. Proven: P1 (run 01M022G18240VX3NX6NRT8HJCF) completed both its tasks this way, producing a 16-file diff, while both git-fetching packages failed. The operator continues to harvest diffs, which AGENTS.md:22 mandates anyway.",
      "task_ref": "t3",
      "reason": "Issue #91 records the sandbox/git_ref decision and its four options; it is a real decision about fleet security posture and about what the q9/c70 carrier can be, and it belongs to the operator. But it does not need to be answered to finish wave 0: nothing in t3/t4/t8/t15 requires the agent to touch git metadata. Deferring it to t6 \u2014 the task that genuinely needs an agent-produced ref \u2014 keeps the decision where its consequences are visible instead of resolving it as a dispatch flag. Cost of this route: briefs carry their own task text, so the ledger sees one opaque instruction rather than a citable document, which is the #79 gap seen from the operator's side and is already recorded.",
      "affects": [
        "t4",
        "t8",
        "t15"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d6",
      "what": "Resolve #91 by measurement rather than by choosing among its four options: codex workspace-write CAN create commits and refs when the dispatch passes -c 'sandbox_workspace_write={writable_roots=[\"<checkout>/.git\"]}'. Keep workspace-write as the dispatch sandbox (option 1 'raise to danger-full-access' and option 4 'drop the git_ref carrier' are both unnecessary), and amend AGENTS.md to permit local ref creation under refs/culture-nodes/<run-id> while keeping the push ban and the never-commit-onto-a-branch rule.",
      "task_ref": "t6",
      "reason": "Measured live on thor with codex-cli 0.147.0: under plain workspace-write 'touch .git/probe-writable' returns 'Read-only file system'; with the scoped writable_roots entry the same probe returns GIT_WRITABLE, and a full write-tree/commit-tree/update-ref sequence produced commit df7d974 at refs/culture-nodes/probe. The confinement widening is one directory on one host with network still off, so the git_ref carrier decided in q9/c70 stands without weakening the sandbox. The policy half was a human decision and the user chose 'permit local refs, keep push banned'.",
      "affects": [
        "c70",
        "t16",
        "t25"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d7",
      "what": "Split t4 in two: the 1000-line lint (tests/lint/filelength_test.go) lands with the four file splits, not before. Land t8's continuation DECLARATION now and keep t8 open for the scheduling integration.",
      "task_ref": "t4",
      "reason": "codex-thor built the lint correctly but did not split the four over-limit files (tracker.py 1523, test_tracker.py 1144, test_run.py 1019, stickiness_ab.py 1016), so the gate is red on arrival. Merging a red gate would make every later merge unable to distinguish new breakage from this known one, which defeats the TDD merge contract. t8 is separable and green, so it merges alone.",
      "affects": [
        "t8"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    },
    {
      "id": "d8",
      "what": "Settle q5: per-artifact read capabilities (binding namespace, run, exact ref, consumer attempt and expiry), with WRITES authorized by the existing attempt callback token. Reject the run-scoped grant (grants every artifact a run accumulated, exceeding the bounded artifact_refs the dispatch already names) and the re-issued attempt token (does not solve cross-attempt reads at all; weakening VerifyFor to accept any same-run attempt erases the boundary the token exists for). Also correct the spec's TTL premise, which measurement falsified.",
      "task_ref": "t5",
      "reason": "Analysis package on codex-orin, run 01M026HSMX5EKGJ89F9NWFNHQG, grounded every claim in cited line numbers and I verified the load-bearing one directly. The spec's q5 instruction says 'a 30-minute session's upload outlives the 15-minute DefaultTokenTTL' -- that is FALSE in the shipped code: internal/worker/dispatch.go:270-300 mints with MintUntil(attemptID, deadline + callbackTokenGrace) whenever a deadline exists, and worker.go:505-510's deadlineFor supplies one from DefaultTimeout when the node omits it, so a 30-minute invocation holds a token to roughly minute 32. DefaultTokenTTL applies only when there is no deadline at all. So no refresh route is needed for the stated case, and the TTL half of q5 dissolves rather than being answered.",
      "affects": [
        "c70",
        "t7",
        "t14"
      ],
      "origin": "user",
      "status": "approved",
      "classification": "acceptable"
    }
  ]
}
